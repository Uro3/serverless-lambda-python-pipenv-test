{
  "name": "serverless-lambda-python-pipenv-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "deploy": "serverless deploy",
    "remove": "serverless remove",
    "invoke:hello": "serverless invoke --function hello -p event.json"
  },
  "devDependencies": {
    "serverless": "^1.65.0",
    "serverless-python-requirements": "^5.1.0"
  }
}
